/**
 * {{dtoName}} - Data Transfer Objects (interface style)
 *
 * Generated by DomainGenerator.
 */
{{#if hasEnums}}
{{#each enums}}
import { {{name}} } from './{{name}}';
{{/each}}

{{/if}}
export interface {{dtoName}} {
  {{#each fields}}
  {{name}}{{#unless required}}?{{/unless}}: {{tsType type}};
  {{/each}}
}

// Derived from {{dtoName}} to avoid duplicating field definitions.
export type {{entityName}}CreateDTO = Omit<{{dtoName}}, 'id'>;

export type {{entityName}}UpdateDTO = Partial<{{dtoName}}>;
